export enum Promotion {
  PlusOne = '1+1',
  Discount = 'discount',
  Gift = 'gift'
}

export enum Resource {
  Category = 'categories',
  Food = 'foods'
}

export enum ResourceAction {
  Get = 'GET'
}
